# Shared Scripts

This folder contains synthetic data generation scripts used by the existing education portfolio batches.

Current scripts:

- `generate_synthetic_data.py`
- `generate_synthetic_batch_2.py`

Future Pipeline C story-specific generation scripts should go in `scripts/pipeline_c_monthly_insights/` unless they are intentionally shared across pipelines.
