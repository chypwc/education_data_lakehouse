# Pipeline B Scripts

This folder contains script-format copies and helpers for the Databricks QA lakehouse pipeline.

The canonical notebook exports remain in `databricks_notebooks/`. The `exported_notebook_scripts/` folder is useful for code review and portfolio readability.
